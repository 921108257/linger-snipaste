# Linger 截图 v0.3.0

修复安装后 GNOME 截图被拒绝的问题，主界面改为后台设置页，加入窗口内部容器自动识别。

## 更新内容

- 修复 GNOME 隐藏窗口后非交互截图被焦点权限拒绝：改用系统支持的交互截图流程；其他桌面拒绝非交互请求时会重试交互模式。取消不会弹出错误窗口。
- 自启动静默驻留托盘；设置页支持全局截图/贴图快捷键、容器识别、登录自启动。保存时检测系统和其他应用快捷键冲突，失败会保留原设置。
- 支持冻结截图中面板、卡片等矩形容器的悬停识别，滚轮切换包围区域，单击选中。拖动仍为自由框选。
- 补齐安装包中遗漏的 D-Bus 原生扩展、GTK 类型库和 Portal 后端依赖；自带 NumPy/OpenCV 识别运行时，图像仅在本机处理。
- 发布产物使用独立目录，避免重复构建把旧安装包嵌入程序。

## 安装或升级

Ubuntu 24.04 / amd64：

```bash
sudo apt install ./linger-snipaste_0.3.0_amd64.deb
```

升级后从旧版托盘退出，再打开新版。默认 F1 截图、F2 贴图，首次启动自动注册；可从托盘“设置…”修改。遇到系统已占用的快捷键，请改用其他组合。

在 GNOME 的系统截图界面选择“屏幕”并完成截图，随后进入自动选区与标注层。系统确认由桌面控制，本版不承诺免确认。

## 验证范围

14 项 Python 测试、8 项 Vue/TypeScript 测试、前端构建与原生 release 构建通过。浏览器验证快捷键录入/保存及普通、窄视口布局。安装包用 Python `-S` 禁用系统 site-packages 验证其私有依赖和后端协议。

真实系统截图请求等待人工确认超时，完整的系统截图确认与全局物理按键尚未通过实机闭环验证。容器识别基于可见图像边界，并非 DOM/控件语义解析；无明显边界时仍需拖动框选。多显示器、分数缩放与非 GNOME 桌面尚未验证。
