# Linger 截图 v0.4.1

修复 GNOME Wayland 下点击“复制并完成”关闭选区后，其他应用无法粘贴图片的问题。

旧版由无窗口的 Python 后台进程写入 GTK 剪贴板；该进程没有收到用户点击或键盘焦点，Wayland 不接受其剪贴板选择请求，但旧代码仍返回成功。新版由当前原生窗口所在进程提供 PNG 数据，剪贴板持有图像的生命周期独立于截图窗口。

- 截图完成和贴图窗口的“复制图片”统一使用原生剪贴板。
- 截图窗口关闭后，后台运行的桌面进程继续提供图片给其他应用。
- 窗口失去焦点或写入失败时保留选区并显示错误，不再由后台组件返回虚假的成功结果。
- 保留 v0.4.0 的直接截图、容器识别和磨砂设置界面。

## 安装

适用 Ubuntu 24.04 / amd64 / GNOME 46：

```bash
sudo apt install ./linger-snipaste_0.4.1_amd64.deb
```

升级后从托盘退出旧版，再打开新版。由 v0.4.0 升级时桌面扩展没有变更，已启用扩展的用户无需再次注销登录。首次安装仍需在设置中启用直接截图扩展并注销登录一次。

## 验证

独立 GNOME 46 Wayland 测试复现了旧后台代码报告成功、其他进程读不到图片的问题。新原生实现关闭来源窗口后，另一个 GTK 应用连续读取两次不同的 73×41 图片，尺寸和像素均一致；无焦点调用会被拒绝。

15 项 Python、10 项 Vue/TypeScript 测试、前端及 Rust release 构建、严格 Clippy、打包依赖检查通过。虚拟桌面测试使用发布代码中的剪贴板函数；实际用户桌面与具体目标应用的粘贴兼容性仍需验证。
