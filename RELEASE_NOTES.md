# Linger 截图 v0.4.0

修复 v0.3.0 先打开 GNOME 截图界面、完成后再进入 Linger 选区的双重流程。

## 更新

- GNOME 46 使用随包桌面扩展一次取图，F1 直接进入 Linger 选区。扩展未启用时明确提示，不再静默回退到第二个系统截图面板。
- 修正“系统截图服务拒绝请求”的误报：系统交互截图没返回文件时，说明可能已取消或未完成，不再笼统归因于权限。
- 去掉设置窗口的系统标题栏；拖动内部标题区移动窗口。“收起到托盘”移到“保存设置”左侧。
- 设置窗口使用半透明磨砂底色；启用配套扩展后由 GNOME 合成器模糊窗口后方内容，仅作用于 Linger 设置窗口。
- 保留窗口内部矩形容器的悬停识别、滚轮选择范围和自由框选。

## 安装与首次启用

适用 Ubuntu 24.04 / amd64 / GNOME 46：

```bash
sudo apt install ./linger-snipaste_0.4.0_amd64.deb
```

先退出旧版 Linger，再打开新版设置，点击“启用直接截图”，然后注销并重新登录一次。GNOME 46 会在登录时发现新安装的扩展。启用此扩展表示允许 Linger 读取单次屏幕截图；不常驻录屏、不上传图像。

登录后默认 F1 直接截图，F2 贴图。扩展只接受 Linger 自己的后台子进程，锁屏时拒绝取图。系统已全面停用扩展时，Linger 不会擅自开启全局扩展开关。

## 验证

15 项 Python 与 8 项 Vue/TypeScript 测试、前端构建、Rust release 构建及严格 Clippy 检查通过。独立 GNOME 46 虚拟桌面实测返回 1280×800 PNG，约 0.9 秒；第二次触发只聚焦现有选区，非 Linger 客户端被拒绝。

配套扩展目前声明支持 GNOME 46。真实桌面安装后注销登录、全局物理按键、多屏和分数缩放仍需进一步验证；容器识别依据可见边界，不保证识别全部控件。
